twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic: /cmd_vel_nav
        timeout: 0.5
        priority: 10
      
      line_follow:
        topic: /cmd_vel_line_follow
        timeout: 0.5
        priority: 50
        
      teleop:
        topic: /cmd_vel_teleop
        timeout: 0.5
        priority: 100
    
    locks:
      lock:
        topic: /twist_mux/lock
        timeout: 0.0
        priority: 255
